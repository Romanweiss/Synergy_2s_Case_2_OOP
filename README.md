````md
# Кейс-задача №2 — Базовый и производный классы (OOP на Python)

## Описание
В рамках кейс-задачи реализована тестовая программа на языке Python, демонстрирующая принципы объектно-ориентированного программирования: наследование, переопределение методов и полиморфизм.

## Условие задачи
Необходимо написать тестовую программу, которая демонстрирует работу методов базового и производного классов.

## Реализация
В ходе выполнения задания реализованы следующие элементы объектно-ориентированного программирования:

- Базовый класс `Employee`, содержащий общие атрибуты и методы
- Производный класс `Developer`, наследующийся от класса `Employee`
- Использование конструктора базового класса через `super()`
- Переопределение методов базового класса:
  - `get_role()`
  - `calc_salary()`
- Реализация дополнительного метода `write_code()`, отсутствующего в базовом классе
- Демонстрация полиморфизма при работе со списком объектов базового типа

## Используемые технологии
- Python 3.x
- Объектно-ориентированное программирование (OOP)

## Алгоритм работы программы
1. Создаётся объект базового класса `Employee`
2. Создаётся объект производного класса `Developer`
3. Выполняются вызовы методов базового и производного классов
4. Демонстрируется переопределение методов
5. Демонстрируется полиморфизм при обращении к объектам через общий интерфейс

## Запуск программы
Для запуска программы выполните команду:

python main.py

## Пример вывода

=== Direct calls ===
Employee 50000.0
Developer(Middle) 85000.0
Roman writes code 🧠💻

=== Polymorphism (same interface, different behavior) ===
Ivan | role=Employee | salary=50000.00
Roman | role=Developer(Middle) | salary=85000.00
```

## Примечание

В программе используется механизм `super()` для вызова конструктора базового класса, а также переопределение методов, что позволяет продемонстрировать основные принципы наследования и полиморфизма в объектно-ориентированном программировании.
